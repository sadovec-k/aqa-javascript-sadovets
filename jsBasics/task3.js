let age = 15;
let isMajor = age >= 18;

console.log("Person age is " + age + ",\n" + "isMajor parameter is " + isMajor);
console.log(`Persone age is ${age}. This person is ${isMajor ? "" : "not"} major.`);
